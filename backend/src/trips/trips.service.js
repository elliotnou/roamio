/**
 * Trips service placeholder.
 *
 * What this file should do:
 * 1. Query trips for authenticated user from Supabase.
 * 2. Insert new trip rows with destination and date range.
 * 3. Fetch a single trip by id with ownership checks.
 * 4. Return normalized entities for route handlers.
 */
export class TripsServicePlaceholder {}
